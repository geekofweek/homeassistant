#################################################################
## Recorder
#################################################################

  purge_keep_days: 14
  db_url: !secret db_url

  exclude:
    domains:
      - updater
      - camera
      - media_player
      - scene
      - script
      - calendar
      - sun
      - weather
      - remote
