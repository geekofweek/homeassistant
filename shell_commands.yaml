#################################################################
## Shell Commands
#################################################################

  export_ha_states: '/home/REDACTED/.homeassistant/hass_saved_states.py -act update_settings -sec {{ section }} -set {{ settings }} -val "{{ value }}"'
  import_ha_states: '/home/REDACTED/.homeassistant/hass_saved_states.py -act update_hass'
